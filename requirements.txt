# Web server
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.9
python-dotenv>=1.0.1

# IANA timezone database — Windows and some minimal Linux images have no
# system tz database, which makes zoneinfo.ZoneInfo(BUSINESS_TIMEZONE) raise.
# This makes it work everywhere regardless of the host OS.
tzdata>=2024.1

# LLM
openai>=1.55.0
# n8n reservation webhook (calendar sync). Already a transitive dependency
# of openai/httpx-based packages, but imported directly so it's pinned here.
httpx>=0.27.0

# Speech-to-text fallback (local, CPU). Pulls ctranslate2 + PyAV — no PyTorch
# needed. Primary STT is OpenAI's gpt-4o-mini-transcribe via the client above.
faster-whisper>=1.1.0
# Decodes/resamples call audio (caller WebM/Opus + assistant WAV) into one
# recording per call. Already pulled in transitively by faster-whisper, but
# recording.py imports it directly so it's pinned here too.
av>=12.0.0

# Text-to-speech (self-hosted, free at any call volume)
piper-tts>=1.3.0

# Retrieval
scikit-learn>=1.5.0
numpy>=1.26.0
pypdf>=5.1.0
